
openlab.register('olmarkdown', function () {

    /*
    var setup = function ($el) {
        var attr = function (s) {
            return $el.attr('data-olmd-' + s);
        };

        var $ta = attr('textarea') ? $(attr('textarea')) : $el.find('textarea');
        $ta.markdown({});
    };
    */


    return setup;
});
